[project]
name = "notesculpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.4.2",
    "keyring>=25.7.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
    "openai>=2.53.0",
]

[project.scripts]
notesculpt = "notesculpt.cli:cli"
